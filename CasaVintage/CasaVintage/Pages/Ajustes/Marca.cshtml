@page
@model CasaVintage.Pages.Ajustes.MarcaModel
@{
    ViewData["Title"] = "Visual identity";
    Layout = "_Layout";
}

<div class="mb-4">
    <span class="kicker">Settings</span>
    <h1 class="mb-1">Visual identity</h1>
    <p class="text-muted mb-0">Upload the store logo. It is shown in the side menu instead of the monogram.</p>
</div>

@if (Model.Mensaje is not null)
{
    <div class="alerta-vintage alerta-ok" role="status">@Model.Mensaje</div>
}

<div class="tarjeta-vintage p-4 p-md-5">
    <form method="post" enctype="multipart/form-data">
        <div asp-validation-summary="All" class="alerta-vintage alerta-error mb-4" role="alert"></div>

        <div class="marca-preview-fila">
            <div class="marca-preview" data-foto-grupo>
                <span class="marca-previa-cuadro" data-foto-previa>
                    @if (Model.LogoActual is not null)
                    {
                        <img src="@Model.LogoActual" alt="Current logo" />
                    }
                    else
                    {
                        <span class="marca-logo marca-logo-grande">V</span>
                    }
                </span>
            </div>
            <div class="marca-preview-texto">
                <label for="Logo" class="form-label">Logo file</label>
                <input type="file" id="Logo" name="Logo" class="form-control" accept=".png,.jpg,.jpeg,.webp" data-foto-input />
                <p class="text-muted mt-2 mb-0" style="font-size:0.85rem;">Formats: PNG, JPG or WEBP. Maximum 8 MB. A PNG with a transparent background is recommended.</p>
            </div>
        </div>

        <div class="d-flex gap-2 flex-wrap mt-4">
            <button type="submit" asp-page-handler="Guardar" class="btn-vintage">Save logo</button>
            @if (Model.LogoActual is not null)
            {
                <button type="submit" asp-page-handler="Quitar" class="btn-vintage-outline" formnovalidate>Remove logo</button>
            }
        </div>
    </form>
</div>

@section Scripts {
    <script src="~/js/personal.js" asp-append-version="true"></script>
}
